app:
  learning-materials-dir: data

server:
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/hsgg
    username: hsgguser
    password: secret

  jpa:
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always
